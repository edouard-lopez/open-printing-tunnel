version: '2'
services:
  daemon:
    restart: always
  backend:
    restart: always
    volumes:
      - backend:/backend
    environment:
      - DEFAULT_INTERFACE=ens192
    command: /usr/bin/supervisord
  frontend:
    restart: always
  nginx:
    restart: always
volumes:
  backend:
  frontend: